We are working in C:\Users\DARRON\OneDrive\Documents\XVatsim.

Before doing anything, read these files in this order:

1. docs\NEXT_SESSION_HANDOFF.md
2. docs\BRAIN_OWNED_RUNTIME_CONTRACT.md
3. docs\LIVE_BATTLE_TEST_RELEASE_GATE_CONTRACT.md
4. docs\BRAIN_OWNED_RUNTIME_AUDIT.md

Treat docs\BRAIN_OWNED_RUNTIME_CONTRACT.md as the bible.

Core rule:

Brain decides. Modules produce facts. UI displays brain-approved facts.

Where we stand:

- The repo is now a single Engineer 3 live core.
- The active runtime path is brain-owned.
- Modules only produce facts.
- The brain owns workflow phase, controller relevance, display intent, final UI
  snapshot, idle/cadence, and fallback eligibility.
- The UI renders only the final brain-approved snapshot.
- Remaining cleanup debt is outside the ordinary live decision path and should
  not distract us from live validation.

Current installed X-Plane plugin:

C:\X-Plane 12\Resources\plugins\XVatsim\win_x64\XVatsim.xpl

Installed SHA256:

1A22A8262A39F6C1FD510FD62A96B5EDB8F55E1E176C4ECC9699B99F60501456

Last verified state:

- Release build passed.
- Full regression harness passed: 234 / 234.
- Git worktree was clean after commit 293c6b2, "Use workflow signals for phase selection".

What was fixed in the last cleanup session:

- Removed raw module-board display ownership, including ModuleBoardSnapshot::displayStations.
- Hardened legacy departure/arrival/enroute board modules as harness-only.
- Moved network plan identity-key construction into brain-owned runtime.
- Quarantined broad route-sector authority proof behind
  ResolveBrainScheduledAuthorityVerification with an explicit schedule reason.
- Removed workflow selection's provisional Controller Relevance board pass.
  Workflow selection now uses narrow WorkflowSignals from radio facts.

Next session goal:

Do live testing with me inside the simulator. I will tell you what xPilot shows,
what XVatsim displays, and how I expect the plugin to behave. You will tail the
logs, correlate my observation to diagnostics, classify the failing Engineer 3
block, and fix only that block.

Live logs to watch:

C:\X-Plane 12\Resources\plugins\XVatsim\logs\xvatsim_diagnostics.log
C:\X-Plane 12\Log.txt

Live behavior contract:

- Any reachable frequency that belongs to our current flight should display once
  the brain identifies it.
- If the controller belongs to the current polygon, display it as current/green.
- If the controller belongs to a future/next polygon, display it as next/orange
  with remaining distance.
- OAK Center / OAK_62_CTR is an important first retest target.
- If xPilot shows a controller but XVatsim does not, determine whether the miss
  is radio board, route polygon, relevance, display intent, UI rendering, or
  stale cache/cadence.
- No ordinary UI refresh may trigger heavy authority proof.
- If a fix is needed, make the smallest Engineer 3 compliant change, then run
  Release build, full harness, install the XPL, update docs, and commit.

Do not start broad architecture cleanup unless I explicitly ask for it. Live
testing is the next path forward.
